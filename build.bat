@echo off
REM Build script for Courseshub - Combines CSS and JS, copies to dist folder
REM Run this script using Git Bash: bash build.sh
REM Or run this batch file which calls the shell script

echo Running build script...
bash build.sh
pause
